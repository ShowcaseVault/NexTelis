[phones]
; Every Number.value is also its PJSIP endpoint id (see ps_endpoints), so
; any all-digit extension dials the matching realtime endpoint directly —
; no dialplan change needed when new numbers are assigned via the backend.
exten => _X.,1,Dial(PJSIP/${EXTEN},30)
 same => n,Hangup()
